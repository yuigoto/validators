import "mocha";
import { expect } from "chai";
import validators from "./index";

const helperList = [
  "Cep",
  "Cnpj",
  "Cpf",
  "CreditCard",
  "DateTime"
];

describe("@yuigoto/validators", () => {
});
