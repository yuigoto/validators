/**
 * type/ValidateMethod
 * ----------------------------------------------------------------------
 * Validates the input of any type, returns a boolean value.
 *
 * @since 0.6.0
 */
export type ValidateMethod<T> = (value: T) => boolean;
